---
title: 使用 shell 命令打开 Windows 特殊文件夹与隐藏目录
tags:
  - Shell
  - Windows
date: 2025-09-06
draft: false
description: Windows Shell 命令大全：快速打开特殊文件夹（如 Startup, AppData）与隐藏目录，提升系统操作效率。
lastmod: 2025-12-28
cover: https://img.linkcf.top/2025/12/20251229123137824.webp
stale: default
---

## 使用 shell 命令打开 Windows 特殊文件夹与隐藏目录

shell:ConnectionsFolder

shell:Desktop

shell:Playlists

shell:ProgramFiles

shell:OneDriveCameraRoll

shell:AppsFolder

shell:CommonRingtones

shell:PrintHood

shell:Local Pictures

shell:AppDataFavorites

shell:CD Burning

shell:3D Objects

shell:Common Start Menu Places

shell:SearchHomeFolder

shell:OEM Links

shell:Device Metadata Store

shell:SyncResultsFolder

shell:SyncCenterFolder

shell:MAPIFolder

shell:VideosLibrary

shell:AppMods

shell:RecycleBinFolder

shell:My Video

shell:Downloads

shell:SystemCertificates

shell:Local Downloads

shell:AccountPictures

shell:Roamed Tile Images

shell:Application Shortcuts

shell:OneDrivePictures

shell:Cookies

shell:UserProgramFiles

shell:PrintersFolder

shell:NetworkPlacesFolder

shell:ChangeRemoveProgramsFolder

shell:Local Videos

shell:Recorded Calls

shell:Quick Launch

shell:MyComputerFolder

shell:Start Menu

shell:SearchTemplatesFolder

shell:Administrative Tools

shell:Contacts

shell:DocumentsLibrary

shell:CommonMusic

shell:SavedPictures

shell:OneDrive

shell:CommonVideo

shell:Fonts

shell:InternetFolder

shell:Programs

shell:Common Startup

shell:ThisDeviceFolder

shell:Common Desktop

shell:AppDataProgramData

shell:SyncSetupFolder

shell:Local Documents

shell:Local AppData

shell:CredentialManager

shell:AppDataDesktop

shell:Common Templates

shell:PublicGameTasks

shell:Common Programs

shell:UsersLibrariesFolder

shell:Links

shell:AppDataDocuments

shell:Ringtones

shell:OneDriveMusic

shell:CameraRollLibrary

shell:DpapiKeys

shell:GameTasks

shell:CommonPictures

shell:CommonDownloads

shell:PhotoAlbums

shell:SavedGames

shell:OneDriveDocuments

shell:History

shell:SearchHistoryFolder

shell:ProgramFilesCommonX86

shell:PicturesLibrary

shell:CSCFolder

shell:Roaming Tiles

shell:AddNewProgramsFolder

shell:RecordedTVLibrary

shell:Templates

shell:UsersFilesFolder

shell:Captures

shell:Common Documents

shell:Development Files

shell:MusicLibrary

shell:ThisPCDesktopFolder

shell:Common AppData

shell:Common Administrative Tools

shell:UserProgramFilesCommon

shell:LocalAppDataLow

shell:Searches

shell:Personal

shell:My Pictures

shell:UserProfiles

shell:Public

shell:AppUpdatesFolder

shell:Windows

shell:System

shell:SystemX86

shell:CryptoKeys

shell:SavedPicturesLibrary

shell:NetHood

shell:Profile

shell:Camera Roll

shell:Libraries

shell:PublicLibraries

shell:Favorites

shell:ImplicitAppShortcuts

shell:Startup

shell:HomeGroupCurrentUserFolder

shell:ConflictFolder

shell:LocalizedResourcesDir

shell:Cache

shell:HomeGroupFolder

shell:Screenshots

shell:Recent

shell:ProgramFilesCommon

shell:Common Start Menu

shell:Local Music

shell:ControlPanelFolder

shell:AppData

shell:User Pinned

shell:ProgramFilesX86

shell:Original Images

shell:SendTo

shell:ResourceDir

shell:PublicAccountPictures

shell:My Music
